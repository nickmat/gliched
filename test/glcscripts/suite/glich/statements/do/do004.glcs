// do004.glcs Test file to test do statement keeps track of line number

write "Start ";

let count = 0;
do while count < 2
  /* Multiline
   * comments
   */
  xxx
  let count = count + 1;
  // Single line comment
  write count + ", ";
loop

/*[OUTPUT]
Start Error (10): Unrecognised statement.

[OUTPUT]*/

// do003.glcs Test file to test do statement keeps track of line number

write "Start ";

let count = 0;
do while count < 2
  /* Multiline
   * comments
   */
  let count = count + 1;
  // Single line comment
  write count + ", ";
loop

xxx
write "end";

/*[OUTPUT]
Start 1, 2, Error (15): Unrecognised statement.

[OUTPUT]*/
